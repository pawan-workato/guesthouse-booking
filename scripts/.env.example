# Copy to scripts/.env and set strong passwords before running npm run seed.
# Never commit scripts/.env — it is gitignored.

SEED_ADMIN_PASSWORD=change-me-admin-min-12-chars
SEED_MANAGER_PASSWORD=change-me-manager-min-12-chars
